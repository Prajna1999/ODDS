[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Open Disaster Data Standard"
authors = [{name = "Prajna", email = "prajna@civicdatalab.in"}]
dynamic = ["version", "description"]
